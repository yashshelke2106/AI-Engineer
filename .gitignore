__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
env/
*.egg-info/

# MLflow store and run outputs — regenerate by running the pipeline.
runs/mlflow.db
mlruns/
catboost_info/

# Saved model artifacts (T0-1) — these are build outputs, not source.
models/
*.joblib
*.pkl

.DS_Store
.idea/
.vscode/

# Lifecycle walkthrough outputs (serve / retrain / gate demo) — regenerate them
# with the README quickstart. Per-dataset validation reports stay tracked.
runs/production/
runs/retraining_frame.csv
runs/*challenger*_report.md
data/concept_*.csv
data/new_customers.csv
