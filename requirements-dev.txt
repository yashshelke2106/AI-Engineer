# Development-only. Kept out of requirements.txt so a serving install does not
# pull a test runner into the deployment image.
-r requirements.txt

pytest==9.0.3
# TestClient needs an HTTP client even though the app is never bound to a port.
httpx==0.28.1
